<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>深入JAVA语言—CAS原理剖析</title>
    <url>/2020/01/05/%E6%B7%B1%E5%85%A5JAVA%E8%AF%AD%E8%A8%80%E2%80%94CAS%E5%8E%9F%E7%90%86%E5%89%96%E6%9E%90/</url>
    <content><![CDATA[<p>JAVA的并发编程包（java.util.concurrent）中的源码大量用到CAS操作。<br>CAS操作使得JAVA可以低成本地实现并发操作。<br>本文将深入探讨CAS操作的实现原理。</p>
<a id="more"></a>

<h2 id="1-什么是CAS操作"><a href="#1-什么是CAS操作" class="headerlink" title="1.什么是CAS操作"></a>1.什么是CAS操作</h2><p>CAS（Compare and Swap），也就是比较并替换，简单说就是有三个值：当前内存值（current value)、内存期望值（expected value)、更新值（updated value),当且仅当当前内存值等于内存期望值的时候，将内存值修改为更新值，并且返回true，否则不作任何操作直接返回false。需要注意的事，该操作是<strong>原子操作</strong>。</p>
<p>例如，AtomicInteger里的compareAndSet方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">     * Atomically sets the value to the given updated value</span></span><br><span class="line"><span class="comment">     * if the current value &#123;<span class="doctag">@code</span> ==&#125; the expected value.</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> expect the expected value</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> update the new value</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span> &#123;<span class="doctag">@code</span> true&#125; if successful. False return indicates that</span></span><br><span class="line"><span class="comment">     * the actual value was not equal to the expected value.</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">compareAndSet</span><span class="params">(<span class="keyword">int</span> expect, <span class="keyword">int</span> update)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> unsafe.compareAndSwapInt(<span class="keyword">this</span>, valueOffset, expect, update);</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h2 id="2-Unsafe类"><a href="#2-Unsafe类" class="headerlink" title="2.Unsafe类"></a>2.Unsafe类</h2><p>JAVA的CAS操作底层都是调用了Unsafe类内部的native方法。这些方法通过JNI调用，直接调用了底层的C语言接口，使得JAVA可以直接操作内存空间。</p>
<p>我们注意到前文提到的unsafe.compareAndSwapInt方法的入参中，有一个静态字段valueOffset。这个字段表示的是value字段在内存相对偏移量，在初始化的时候获取。如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">long</span> valueOffset;</span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> &#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        valueOffset = unsafe.objectFieldOffset</span><br><span class="line">            (AtomicInteger.class.getDeclaredField("value"));</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Exception ex) &#123; <span class="keyword">throw</span> <span class="keyword">new</span> Error(ex); &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>所有的unsafe的native方法，追踪到hotspot源码，都在hotspot/src/share/vm/prims/unsafe.cpp中实现。</p>
<h2 id="3-获取字段的字节偏移量"><a href="#3-获取字段的字节偏移量" class="headerlink" title="3.获取字段的字节偏移量"></a>3.获取字段的字节偏移量</h2><p>先来看unsafe.objectFieldOffset，我对C++不是特别熟悉，但从语义上其实可以看出，这个方法是根据字段类型<strong>获取其相对偏移字节数</strong>。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// hotspot/src/share/vm/prims/unsafe.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">inline</span> jlong <span class="title">field_offset_from_byte_offset</span><span class="params">(jlong byte_offset)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> byte_offset;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function">jint <span class="title">find_field_offset</span><span class="params">(jobject field, <span class="keyword">int</span> must_be_static, TRAPS)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (field == <span class="literal">NULL</span>) &#123;</span><br><span class="line">    THROW_0(vmSymbols::java_lang_NullPointerException());</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  oop reflected   = JNIHandles::resolve_non_null(field);</span><br><span class="line">  oop mirror      = java_lang_reflect_Field::clazz(reflected);</span><br><span class="line">  Klass* k      = java_lang_Class::as_Klass(mirror);</span><br><span class="line">  <span class="keyword">int</span> slot        = java_lang_reflect_Field::slot(reflected);</span><br><span class="line">  <span class="keyword">int</span> modifiers   = java_lang_reflect_Field::modifiers(reflected);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (must_be_static &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="keyword">int</span> really_is_static = ((modifiers &amp; JVM_ACC_STATIC) != <span class="number">0</span>);</span><br><span class="line">    <span class="keyword">if</span> (must_be_static != really_is_static) &#123;</span><br><span class="line">      THROW_0(vmSymbols::java_lang_IllegalArgumentException());</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">int</span> offset = InstanceKlass::cast(k)-&gt;field_offset(slot);</span><br><span class="line">  <span class="keyword">return</span> field_offset_from_byte_offset(offset);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">UNSAFE_ENTRY(jlong, Unsafe_ObjectFieldOffset(JNIEnv *env, jobject unsafe, jobject field))</span><br><span class="line">  UnsafeWrapper(<span class="string">"Unsafe_ObjectFieldOffset"</span>);</span><br><span class="line">  <span class="keyword">return</span> find_field_offset(field, <span class="number">0</span>, THREAD);</span><br><span class="line">UNSAFE_END</span><br></pre></td></tr></table></figure>

<h2 id="4-原子操作的实现"><a href="#4-原子操作的实现" class="headerlink" title="4.原子操作的实现"></a>4.原子操作的实现</h2><p>然后接着来看compareAndSwapInt的native实现，先是利用上文获取的字节偏移量offset，获取需要进行CAS操作的对象所在的地址。这部分功能在index_oop_from_field_offset_long中实现，此处不作展开。此外可以看到，<br>该方法接着通过 Atomic::cmpxchg 来实现比较和替换操作，其中参数x是即将更新的值，参数e是原内存的值。如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">UNSAFE_ENTRY(jboolean, Unsafe_CompareAndSwapInt(JNIEnv *env, jobject unsafe, jobject obj, jlong offset, jint e, jint x))</span><br><span class="line">  UnsafeWrapper(&quot;Unsafe_CompareAndSwapInt&quot;);</span><br><span class="line">  oop p &#x3D; JNIHandles::resolve(obj);</span><br><span class="line">  jint* addr &#x3D; (jint *) index_oop_from_field_offset_long(p, offset);</span><br><span class="line">  return (jint)(Atomic::cmpxchg(x, addr, e)) &#x3D;&#x3D; e;</span><br><span class="line">UNSAFE_END</span><br></pre></td></tr></table></figure>

<p>最关键的原子操作Atomic::cmpxchg。这个方法在不同的操作系统中实现不一样，这里以linux x86为例：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// hotspot/src/os_cpu/linux_x86/vm/atomic_linux_x86.inline.hpp</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Adding a lock prefix to an instruction on MP machine</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LOCK_IF_MP(mp) <span class="meta-string">"cmp $0, "</span> #mp <span class="meta-string">"; je 1f; lock; 1: "</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">inline</span> jint     Atomic::cmpxchg    (jint     exchange_value, <span class="keyword">volatile</span> jint*     dest, jint     compare_value) &#123;</span><br><span class="line">  <span class="keyword">int</span> mp = os::is_MP();</span><br><span class="line">  __<span class="function">asm__ <span class="title">volatile</span> <span class="params">(LOCK_IF_MP(%<span class="number">4</span>) <span class="string">"cmpxchgl %1,(%3)"</span></span></span></span><br><span class="line"><span class="function"><span class="params">                    : <span class="string">"=a"</span> (exchange_value)</span></span></span><br><span class="line"><span class="function"><span class="params">                    : <span class="string">"r"</span> (exchange_value), <span class="string">"a"</span> (compare_value), <span class="string">"r"</span> (dest), <span class="string">"r"</span> (mp)</span></span></span><br><span class="line"><span class="function"><span class="params">                    : <span class="string">"cc"</span>, <span class="string">"memory"</span>)</span></span>;</span><br><span class="line">  <span class="keyword">return</span> exchange_value;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>LOCK_IF_MP 表示如果是多处理器，则添加一个lock指令作为前缀，反之，就省略lock前缀（单处理器会不需要lock前缀提供的内存屏障效果）。这里的lock前缀就是使用了处理器的总线锁（最新的处理器都使用缓存锁代替总线锁来提高性能）。从而，实现了原子地交换更新值。</p>
<h2 id="5-CAS操作的几个问题"><a href="#5-CAS操作的几个问题" class="headerlink" title="5.CAS操作的几个问题"></a>5.CAS操作的几个问题</h2><h3 id="5-1-ABA问题"><a href="#5-1-ABA问题" class="headerlink" title="5.1 ABA问题"></a>5.1 ABA问题</h3><p>CAS操作只有在检查值等于期望值的时候，才会发生替换，但是如果期间发生了两次替换，使得值从A–&gt;B–&gt;A，虽然检查值依然是A，但是期间其实发生了变化。因此，CAS无法检测到ABA问题。</p>
<p>解决办法就是给每个值附带上版本号，每次更新数据都要递增版本号。</p>
<p>因此，JDK1.5中引入了AtomicStampedReference类。如它的compareAndSet方法，需要指定期望版本号expectedStamp，只有在版本号对得上的情况下，才会发生值替换。如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">    * Atomically sets the value of both the reference and stamp</span></span><br><span class="line"><span class="comment">    * to the given update values if the</span></span><br><span class="line"><span class="comment">    * current reference is &#123;<span class="doctag">@code</span> ==&#125; to the expected reference</span></span><br><span class="line"><span class="comment">    * and the current stamp is equal to the expected stamp.</span></span><br><span class="line"><span class="comment">    *</span></span><br><span class="line"><span class="comment">    * <span class="doctag">@param</span> expectedReference the expected value of the reference</span></span><br><span class="line"><span class="comment">    * <span class="doctag">@param</span> newReference the new value for the reference</span></span><br><span class="line"><span class="comment">    * <span class="doctag">@param</span> expectedStamp the expected value of the stamp</span></span><br><span class="line"><span class="comment">    * <span class="doctag">@param</span> newStamp the new value for the stamp</span></span><br><span class="line"><span class="comment">    * <span class="doctag">@return</span> &#123;<span class="doctag">@code</span> true&#125; if successful</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">   <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">compareAndSet</span><span class="params">(V   expectedReference,</span></span></span><br><span class="line"><span class="function"><span class="params">                                V   newReference,</span></span></span><br><span class="line"><span class="function"><span class="params">                                <span class="keyword">int</span> expectedStamp,</span></span></span><br><span class="line"><span class="function"><span class="params">                                <span class="keyword">int</span> newStamp)</span> </span>&#123;</span><br><span class="line">       Pair&lt;V&gt; current = pair;</span><br><span class="line">       <span class="keyword">return</span></span><br><span class="line">           expectedReference == current.reference &amp;&amp;</span><br><span class="line">           expectedStamp == current.stamp &amp;&amp;</span><br><span class="line">           ((newReference == current.reference &amp;&amp;</span><br><span class="line">             newStamp == current.stamp) ||</span><br><span class="line">            casPair(current, Pair.of(newReference, newStamp)));</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>

<h3 id="5-2-自旋耗时问题"><a href="#5-2-自旋耗时问题" class="headerlink" title="5.2 自旋耗时问题"></a>5.2 自旋耗时问题</h3><p>CAS操作一般是在循环里不断重试，直到成功才中断循环。在并发量高的情况下，这种自旋会增加循环次数，从而增加耗时。比如CountDownLatch类里有如下代码：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">boolean</span> <span class="title">tryReleaseShared</span><span class="params">(<span class="keyword">int</span> releases)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// Decrement count; signal when transition to zero</span></span><br><span class="line">    <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">        <span class="keyword">int</span> c = getState();</span><br><span class="line">        <span class="keyword">if</span> (c == <span class="number">0</span>)</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">        <span class="keyword">int</span> nextc = c-<span class="number">1</span>;</span><br><span class="line">        <span class="keyword">if</span> (compareAndSetState(c, nextc))</span><br><span class="line">            <span class="comment">// 不断重试，直到更新成功才中断循环</span></span><br><span class="line">            <span class="keyword">return</span> nextc == <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="5-3-单个变量问题"><a href="#5-3-单个变量问题" class="headerlink" title="5.3 单个变量问题"></a>5.3 单个变量问题</h3><p>上述提到的CAS操作只适合单个变量。如果要同时对多个变量进行CAS操作，就无能为力了。为此JDK1.5中有AtomicReference类，这个类支持对一个复杂对象的原子操作，底层同样适用了unsafe。</p>
]]></content>
      <categories>
        <category>JAVA</category>
      </categories>
      <tags>
        <tag>CAS原理</tag>
      </tags>
  </entry>
  <entry>
    <title>2020大事记</title>
    <url>/2020/01/01/2020%E5%A4%A7%E4%BA%8B%E8%AE%B0/</url>
    <content><![CDATA[<ul>
<li><p>2020年1月 <a href="https://bkso.baidu.com/item/2020%E5%B9%B4%E7%BE%8E%E4%BC%8A%E5%86%B2%E7%AA%81/24251949" target="_blank" rel="noopener">美国伊朗冲突</a>加剧，美国无人机狙杀伊朗圣城旅指挥官苏莱曼尼，引发伊朗强烈反应。后伊朗以导弹袭击美国驻伊拉克军事基地作为报复，报复行动中错误击落民航班机，导致<a href="https://zh.wikipedia.org/wiki/%E4%B9%8C%E5%85%8B%E5%85%B0%E5%9B%BD%E9%99%85%E8%88%AA%E7%A9%BA752%E5%8F%B7%E7%8F%AD%E6%9C%BA%E7%A9%BA%E9%9A%BE" target="_blank" rel="noopener">乌克兰国际航空752号班机空难</a>。</p>
</li>
<li><p>2020年1月-至今，<a href="https://zh.wikipedia.org/wiki/2019%E6%96%B0%E5%9E%8B%E5%86%A0%E7%8B%80%E7%97%85%E6%AF%92" target="_blank" rel="noopener">2019新型冠状病毒</a>爆发，并传播至世界各地。武汉多地封城隔离，国务院延长春节假期，全国人民团结抗击肺炎病毒。</p>
</li>
<li><p>2020年1月16日，中美两国签署第一阶段贸易协议。长达19个月的<a href="https://zh.wikipedia.org/zh/2018%EF%BC%8D2020%E5%B9%B4%E4%B8%AD%E7%BE%8E%E8%B4%B8%E6%98%93%E6%88%98" target="_blank" rel="noopener">中美贸易战</a>，画上阶段性句号。</p>
</li>
<li><p>2020年1月31日，英国已于2020年1月31日晚上11时(GMT)正式脱离欧盟，随后进入脱欧过渡期，至2020年12月31日结束。持续三年半的<a href="https://zh.wikipedia.org/wiki/%E8%8B%B1%E5%9C%8B%E8%84%AB%E6%AD%90" target="_blank" rel="noopener">英国脱欧</a>进程落下帷幕。</p>
</li>
</ul>
]]></content>
      <categories>
        <category>历史</category>
      </categories>
      <tags>
        <tag>历史</tag>
      </tags>
  </entry>
  <entry>
    <title>Azkaban 源码分析和插件实现</title>
    <url>/2017/09/21/Azkaban-%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90%E5%92%8C%E6%8F%92%E4%BB%B6%E5%AE%9E%E7%8E%B0/</url>
    <content><![CDATA[<h2 id="Azkaban简介"><a href="#Azkaban简介" class="headerlink" title="Azkaban简介"></a>Azkaban简介</h2><blockquote>
<p><strong>Azkaban</strong> was implemented at LinkedIn to solve the problem of Hadoop job dependencies. We had jobs that needed to run in  order, from ETL jobs to data analytics products.<br>Initially a single server solution, with the increased number of Hadoop users over the years, Azkaban has evolved to be a more robust solution.</p>
</blockquote>
<blockquote>
<p>Azkaban 是由Linkedln公司为了解决hadoop 作业之间的依赖而实现的。因为有一些ETL作业以及数据分析产品需要按照一定的顺序去执行。<br>随着hadoop用户的逐年增加，Azkaban从一个简单的服务解决方案发展成为一个更加健壮鲁棒的方案。</p>
</blockquote>
<a id="more"></a>

<h2 id="azkaban架构分析"><a href="#azkaban架构分析" class="headerlink" title="azkaban架构分析"></a>azkaban架构分析</h2><p>&emsp;Azkaban的系统架构非常简单主要由三个组件组成：</p>
<ul>
<li><strong>WebServer</strong> ：提供API，提供分发作业、调度作业和监控报警等功能；</li>
<li><strong>Executor</strong> ：对WebServer 暴露 API ，提供执行作业的功能，支持作业插件；</li>
<li><strong>MySQL</strong> ：数据存储，实现Web 和 Exec之间的数据共享和部分状态的同步。<br><img src="https://pic-bed-sz.oss-cn-shenzhen.aliyuncs.com/blog/azkaban%E6%9E%B6%E6%9E%84%E5%9B%BE.png" alt="azkaban架构图"></li>
</ul>
<h2 id="Azkaban-WebServer"><a href="#Azkaban-WebServer" class="headerlink" title="Azkaban WebServer"></a>Azkaban WebServer</h2><p>&emsp;对azkaban WebServer的各个功能详细介绍如下：</p>
<h3 id="提供API调用"><a href="#提供API调用" class="headerlink" title="提供API调用"></a>提供API调用</h3><p>&emsp;在azkaban-webserver工程中，可以非常清晰地看到对外暴露的Servlet，其中最主要的几个是：</p>
<ul>
<li><strong>ExecutorServlet</strong> 主要提供立即执行作业、取消作业流、暂停作业流、获取流或节点日志等接口</li>
<li><strong>ScheduleServlet</strong> 主要提供设置调度、设置Sla报警规则、获取调度信息等接口</li>
<li><strong>HistoryServlet</strong> 主要提供查看作业流执行历史的接口</li>
<li><strong>ProjectManagerServlet</strong> 主要提供上传项目zip包、下载项目zip包、删除项目、获取流的DAG等接口<h3 id="分发作业"><a href="#分发作业" class="headerlink" title="分发作业"></a>分发作业</h3></li>
</ul>
<p>&emsp;ExecutorManager 主要承担这部分的功能，所有类型的作业（包括立即执行和调度执行）,都会通过<em>submitExecutableFlow(ExecutableFlow exflow, String userId)</em>这个方法进行提交。</p>
<p>&emsp;在该方法中，我们可以看到：如果是多执行节点模式下，执行实例先放进分发队列中，然后通过线程定时取出实例<em>（processQueuedFlows）</em>，选择某个执行节点之后，再通过<em>dispatch</em>方法进行执行分发<em>（selectExecutorAndDispatchFlow）</em>；如果是单执行节点模式下，就立即调用<em>dispatch</em>方法进行分发。生产环境一般采用多节点模式。核心代码如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (isMultiExecutorMode()) &#123;   <span class="comment">//多节点模式，放进队列，等待线程轮询</span></span><br><span class="line">    <span class="comment">//Take MultiExecutor route</span></span><br><span class="line">    executorLoader.addActiveExecutableReference(reference);</span><br><span class="line">    queuedFlows.enqueue(exflow, reference);</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;                       <span class="comment">//单节点模式，直接调用dispatch方法执行</span></span><br><span class="line">    <span class="comment">// assign only local executor we have</span></span><br><span class="line">    Executor choosenExecutor = activeExecutors.iterator().next();</span><br><span class="line">    executorLoader.addActiveExecutableReference(reference);</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        dispatch(reference, exflow, choosenExecutor);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (ExecutorManagerException e) &#123;</span><br><span class="line">        executorLoader.removeActiveExecutableReference(reference.getExecId());</span><br><span class="line">            <span class="keyword">throw</span> e;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;从上边看到执行实例都经过了dispatch方法。因为Web 和Exec之间是通过mysql进行数据共享的，大部分的数据都在dispatch之前预先写进了mysql。所以dispatch进行作业分发的逻辑非常简单，就是简单地通过HTTP请求传递实例的execId等信息，其余所需要的数据都通过数据库读写完成传递。</p>
<h3 id="调度作业"><a href="#调度作业" class="headerlink" title="调度作业"></a>调度作业</h3><p>&emsp;调度作业是调度系统的最重要的功能之一，也是Azkaban里相对复杂的一个模块。调度是通过ScheduleManager对外暴露，对应着的结构是Schedule；对内是通过TriggerManager来实现，对应着的结构是Trigger。</p>
<p>&emsp;所有的调度信息都通过ScheduleManager.scheduleFlow传入,可以看到传入参数包含了项目id、项目名字、流名字、第一次调度时间戳、时区、调度周期、下一次执行时间、提交时间、提交人。对于一个调度来说，最关键的信息无非是第一次调度时间和调度周期。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public Schedule scheduleFlow(final int scheduleId, final int projectId,</span><br><span class="line">      final String projectName, final String flowName, final String status,</span><br><span class="line">      final long firstSchedTime, final DateTimeZone timezone,</span><br><span class="line">      final ReadablePeriod period, final long lastModifyTime,</span><br><span class="line">      final long nextExecTime, final long submitTime, final String submitUser)</span><br></pre></td></tr></table></figure>

<p>&emsp;从scheduleFlow 往下可以看到调用了TriggerBasedScheduleLoader.insertSchedule。这个方法里边先是将Schedule转换成了Trigger，然后将Trigger放到了TriggerManager里边。scheduleToTrigger方法写的非常简洁巧妙，读者可自行鉴赏，此处不作细致分析。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"> <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">insertSchedule</span><span class="params">(Schedule s)</span> <span class="keyword">throws</span> ScheduleManagerException </span>&#123;</span><br><span class="line">   Trigger t = scheduleToTrigger(s);</span><br><span class="line">   <span class="keyword">try</span> &#123;</span><br><span class="line">     triggerManager.insertTrigger(t, t.getSubmitUser());</span><br><span class="line">     s.setScheduleId(t.getTriggerId());</span><br><span class="line">   &#125; <span class="keyword">catch</span> (TriggerManagerException e) &#123;</span><br><span class="line">     <span class="keyword">throw</span> <span class="keyword">new</span> ScheduleManagerException(<span class="string">"Failed to insert new schedule!"</span>, e);</span><br><span class="line">   &#125;</span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;我们在继续看看Trigger被塞到TriggerManager做了些啥。从下边可以看到，先是调用triggerLoader写进数据库，然后就放到了一个线程runnerThread中去轮询了。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">insertTrigger</span><span class="params">(Trigger t)</span> <span class="keyword">throws</span> TriggerManagerException </span>&#123;</span><br><span class="line">    <span class="keyword">synchronized</span> (syncObj) &#123;</span><br><span class="line">      <span class="keyword">try</span> &#123;</span><br><span class="line">        triggerLoader.addTrigger(t);</span><br><span class="line">      &#125; <span class="keyword">catch</span> (TriggerLoaderException e) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> TriggerManagerException(e);</span><br><span class="line">      &#125;</span><br><span class="line">      runnerThread.addTrigger(t);</span><br><span class="line">      triggerIdMap.put(t.getTriggerId(), t);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;接下来就显而易见了，这个线程TriggerScannerThread  runnerThread 定期检查Trigger是否应该触发（onTriggerTrigger）或者终止（onTriggerExpire）。</p>
<p>&emsp;Trigger触发的时候就会调用自己的action.doAction()，调度任务的Trigger的action一般都是ExecuteFlowAction，其doAction方法主要做了两个事情，第一个是构建执行实例ExecutableFlow，第二个是如果该调度设置了报警规则，则构建SlaTrigger，并将trigger放进线程runnerThread 里。</p>
<p>&emsp;构建执行实例完成后，可以看到调用了executorManager.submitExecutableFlow(exflow, submitUser) 进行作业分发，这样子，就跟上文提到的作业分发殊途同归。下边不再分析。</p>
<h3 id="WebServer总结"><a href="#WebServer总结" class="headerlink" title="WebServer总结"></a>WebServer总结</h3><p>&emsp;下边用一张图简单总结代码流程：<br><img src="https://pic-bed-sz.oss-cn-shenzhen.aliyuncs.com/blog/azkaban%E4%BB%A3%E7%A0%81%E6%B5%81%E7%A8%8B.png" alt="image.png"></p>
<h2 id="Azkaban-Executor"><a href="#Azkaban-Executor" class="headerlink" title="Azkaban Executor"></a>Azkaban Executor</h2><p>&emsp;对Azkaban Executor的各个功能详细介绍如下：</p>
<h3 id="提供-API-调用"><a href="#提供-API-调用" class="headerlink" title="提供 API 调用"></a>提供 API 调用</h3><p>  Azkaban3.0后就开始支持多执行节点部署。单个执行节点比较简单，对web暴露的API也比较少，主要是：</p>
<ul>
<li><strong>ExecutorServlet</strong> 主要提供执行、取消、暂停、日志查询等接口。</li>
</ul>
<h3 id="执行作业"><a href="#执行作业" class="headerlink" title="执行作业"></a>执行作业</h3><p>&emsp;上边提及到WebServer通过dispatch方法进行作业分发。这里简单看下执行节点接收到作业执行请求之后的流程是怎样的。我们在ExecutorServlet中看到所有的执行作业请求都经过handleAjaxExecute方法，这个方法简单地将实例唯一的execId传递给FlowRunnerManager：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">handleAjaxExecute</span><span class="params">(HttpServletRequest req,</span></span></span><br><span class="line"><span class="function"><span class="params">      Map&lt;String, Object&gt; respMap, <span class="keyword">int</span> execId)</span> <span class="keyword">throws</span> ServletException </span>&#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">      flowRunnerManager.submitFlow(execId);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (ExecutorManagerException e) &#123;</span><br><span class="line">      e.printStackTrace();</span><br><span class="line">      logger.error(e);</span><br><span class="line">      respMap.put(RESPONSE_ERROR, e.getMessage());</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;FlowRunnerManager 通过submitFlow方法提交工作流去执行。先是构建执行实例ExecutableFlow，然后准备执行目录setupFlow(flow)，然后生成FlowRunner，然后提交到线程池去运行executorService.submit(runner)。（代码颇长，略过）</p>
<p>&emsp;FlowRunner本身实现了Runnable，其run方法里边调用了 runFlow方法，runFlow方法内容如下。方法里按照Flow的层次结构以宽度优先的顺序访问DAG图的每一个job，并逐个job提交执行。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">runFlow</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">    logger.info(<span class="string">"Starting flows"</span>);</span><br><span class="line">    runReadyJob(<span class="keyword">this</span>.flow);</span><br><span class="line">    updateFlow();</span><br><span class="line">    <span class="keyword">while</span> (!flowFinished) &#123;</span><br><span class="line">      <span class="keyword">synchronized</span> (mainSyncObj) &#123;</span><br><span class="line">        <span class="keyword">if</span> (flowPaused) &#123;</span><br><span class="line">          <span class="keyword">try</span> &#123;</span><br><span class="line">            mainSyncObj.wait(CHECK_WAIT_MS);</span><br><span class="line">          &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">          &#125;</span><br><span class="line">          <span class="keyword">continue</span>;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">          <span class="keyword">if</span> (retryFailedJobs) &#123;</span><br><span class="line">            retryAllFailures();</span><br><span class="line">          &#125; <span class="keyword">else</span> <span class="keyword">if</span> (!progressGraph()) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">              mainSyncObj.wait(CHECK_WAIT_MS);</span><br><span class="line">            &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">            &#125;</span><br><span class="line">          &#125;</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    executorService.shutdown();</span><br><span class="line">    updateFlow();</span><br><span class="line">    logger.info(<span class="string">"Finished Flow"</span>);</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;对于单个job，最后构造一个JobRunner去执行执行这个作业，每个JobRunner也是实现了Runnable。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> JobRunner <span class="title">createJobRunner</span><span class="params">(ExecutableNode node)</span> </span>&#123;</span><br><span class="line">   File path = <span class="keyword">new</span> File(execDir, node.getJobSource());</span><br><span class="line"></span><br><span class="line">   JobRunner jobRunner =</span><br><span class="line">       <span class="keyword">new</span> JobRunner(node, path.getParentFile(), executorLoader,</span><br><span class="line">           jobtypeManager);</span><br><span class="line">   <span class="keyword">if</span> (watcher != <span class="keyword">null</span>) &#123;</span><br><span class="line">     jobRunner.setPipeline(watcher, pipelineLevel);</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">if</span> (validateUserProxy) &#123;</span><br><span class="line">     jobRunner.setValidatedProxyUsers(proxyUsers);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   jobRunner.setDelayStart(node.getDelayedExecution());</span><br><span class="line">   jobRunner.setLogSettings(logger, jobLogFileSize, jobLogNumFiles);</span><br><span class="line">   jobRunner.addListener(listener);</span><br><span class="line"></span><br><span class="line">   <span class="keyword">if</span> (JobCallbackManager.isInitialized()) &#123;</span><br><span class="line">     jobRunner.addListener(JobCallbackManager.getInstance());</span><br><span class="line">   &#125;</span><br><span class="line">   configureJobLevelMetrics(jobRunner);</span><br><span class="line">   <span class="keyword">return</span> jobRunner;</span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;每个jobRunner在执行的时候，都去插件模块里边寻找对应的插件来进行job的类型加载。每种job类型都有对应的run方法。所有就是<strong>通过反射去调用各个插件类的run方法去执行各个job</strong>。Azkaban的插件类型包括默认类型（script、python、java、cmd 等）以及azkaban-plugin项目下的一些自定义类型（hive、mr、spark等）。下一个章节会详细介绍插件的实现原理。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">       job = jobtypeManager.buildJobExecutor(<span class="keyword">this</span>.jobId, props, logger);</span><br><span class="line">     &#125; <span class="keyword">catch</span> (JobTypeManagerException e) &#123;</span><br><span class="line">       logger.error(<span class="string">"Failed to build job type"</span>, e);</span><br><span class="line">       <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">     &#125;</span><br></pre></td></tr></table></figure>
<h2 id="Azkaban-Plugin"><a href="#Azkaban-Plugin" class="headerlink" title="Azkaban Plugin"></a>Azkaban Plugin</h2><h3 id="插件机制的实现原理"><a href="#插件机制的实现原理" class="headerlink" title="插件机制的实现原理"></a>插件机制的实现原理</h3><p>&emsp;Azkaban的插件机制使得可以执行各种复杂类型的作业，并且实现了调度功能和作业类型的松耦合。例如 ，对于MR类型和Spark类型的插件，类继承关系图如下。</p>
<p><img src="https://pic-bed-sz.oss-cn-shenzhen.aliyuncs.com/blog/azkaban%E4%BD%9C%E4%B8%9A%E7%B1%BB%E7%BB%A7%E6%89%BF%E5%85%B3%E7%B3%BB.png" alt="azkaban作业类继承关系"></p>
<p>&emsp;每种插件作业都会单独起一个进程去执行。其中ProcessJob主要职能就是新建进程,并执行指定的命令command；JavaProcessJob继承自它，且将command命令特化为 java，也就是新建一个java进程；其他的hadoop插件又各自继承自JavaProcessJob。<br>&emsp;如果要自己实现插件类，只要继承JavaProcessJob类，并在插件类的run方法里，指定新建的java进程的主类等信息，即可。</p>
<h3 id="基于HiveServer2的Azkaban插件的实现思路"><a href="#基于HiveServer2的Azkaban插件的实现思路" class="headerlink" title="基于HiveServer2的Azkaban插件的实现思路"></a>基于HiveServer2的Azkaban插件的实现思路</h3><p>&emsp;了解了插件机制的实现原理后，我们尝试实现一个基于HiveServer2的Azkaban插件。</p>
<h4 id="HiveServer-amp-HiveSever2"><a href="#HiveServer-amp-HiveSever2" class="headerlink" title="HiveServer &amp; HiveSever2"></a>HiveServer &amp; HiveSever2</h4><p>&emsp;先来介绍HiveServer，原名是Thrift server。HiveServer 是一个服务端，允许远程客户端通过请求提交hive作业或者获取作业结果。HiveSever是基于Thrift框架实现的，但后来的HiveServer2也是基于Thrift框架，所以命名上从Thrift Server更名为HiveServer.<br>&emsp;HiveServer2要比HiveSever更加优秀，支持高并发和安全认证。HiveServer已经不被推荐使用。<br>&emsp;仔细研究Azkaban的Hive插件的代码，我们可以知道，Azkaban是通过Hive Client来提交Hive作业的，也就是下图中的CLI方式。这种方式的问题还是挺多的，由于直接绕过了HiveServer2，所以不支持高并发和细粒度的权限控制等，存在很多隐患。</p>
<p><img src="https://pic-bed-sz.oss-cn-shenzhen.aliyuncs.com/blog/hiveSQL%E6%89%A7%E8%A1%8C%E6%96%B9%E5%BC%8F.png" alt="hiveSQL执行方式"></p>
<h4 id="HiveServer2提供的API"><a href="#HiveServer2提供的API" class="headerlink" title="HiveServer2提供的API"></a>HiveServer2提供的API</h4><p>&emsp;HiveServer2提交作业其实是通过JDBC方式来提交的，那我们来看下HiveServer2都提供了哪些api：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">同步提交HQL</span><br><span class="line">ThriftCLIServiceClient.executeStatement(SessionHandle sessionHandle, String statement, Map confOverlay) <span class="keyword">throws</span> HiveSQLException</span><br><span class="line"></span><br><span class="line">异步提交HQL</span><br><span class="line">ThriftCLIServiceClient.executeStatementAsync(SessionHandle sessionHandle, String statement, Map confOverlay) <span class="keyword">throws</span> HiveSQLException</span><br><span class="line"></span><br><span class="line">请求日志或者结果</span><br><span class="line">ThriftCLIServiceClient.fetchResults(OperationHandle opHandle, FetchOrientation orientation, <span class="keyword">long</span> maxRows, FetchType fetchType) <span class="keyword">throws</span> HiveSQLException</span><br><span class="line">(FetchType分为FetchType.LOG 和 FetchType.QUERY_OUTPUT，分别对应日志和结果)</span><br><span class="line"></span><br><span class="line">请求执行状态</span><br><span class="line">ThriftCLIServiceClient.getOperationStatus(OperationHandle opHandle) <span class="keyword">throws</span> HiveSQLException \</span><br><span class="line">(状态包括：INITIALIZED RUNNING FINISHED CANCELED CLOSED ERROR UNKNOWN PENDING)</span><br><span class="line"></span><br><span class="line">取消执行</span><br><span class="line">ThriftCLIServiceClient.cancelOperation(OperationHandle opHandle) <span class="keyword">throws</span> HiveSQLException</span><br><span class="line"></span><br><span class="line">关闭句柄</span><br><span class="line">ThriftCLIServiceClient.closeOperation(OperationHandle opHandle) <span class="keyword">throws</span> HiveSQLException \</span><br><span class="line">每执行完一条sql都要关闭句柄</span><br></pre></td></tr></table></figure>

<p>在上述接口的基础上，还封装了类JDBC的接口，从而可以用常规的访问RDS数据库去访问HiveServer2。例如：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Class.forName(<span class="string">"org.apache.hive.jdbc.HiveDriver"</span>);</span><br><span class="line">connection = DriverManager.getConnection(hiveServerUrl);</span><br><span class="line">hiveStatement = (HiveStatement) connection.createStatement();</span><br><span class="line">hiveStatement.execute(sql);</span><br></pre></td></tr></table></figure>
<p>另外还提供了异步获取日志的办法：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">while (hiveStat !&#x3D; null &amp;&amp; !hiveStat.isClosed())&#123;</span><br><span class="line">     if(hiveStat.hasMoreLogs())&#123;</span><br><span class="line">        for(String log : hiveStat.getQueryLog())&#123;</span><br><span class="line">           logger.info(log);</span><br><span class="line">        &#125;</span><br><span class="line">     &#125;</span><br><span class="line">     Thread.sleep(QUERY_LOG_TIMEOUT);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="具体实现"><a href="#具体实现" class="headerlink" title="具体实现"></a>具体实现</h4><p>&emsp;有了上述的基础，基于HiveServer2的Azkaban插件的实现就清晰了。</p>
<ul>
<li>实现继承JavaProcessJob的HadoopHiveJob类，实现run方法，并指定新建的Java进程的主类,如下。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> String HADOOP_SECURE_HIVE_WRAPPER = <span class="string">"azkaban.jobtype.HadoopSecureHiveWrapper"</span>;</span><br><span class="line"><span class="comment">//此处省略N行</span></span><br><span class="line"></span><br><span class="line"> <span class="meta">@Override</span></span><br><span class="line">  <span class="function"><span class="keyword">protected</span> String <span class="title">getJavaClass</span><span class="params">()</span> </span>&#123;</span><br><span class="line">	<span class="keyword">if</span> (jobProps.containsKey(JAVA_CLASS)) <span class="keyword">return</span> jobProps.get(JAVA_CLASS);</span><br><span class="line">	<span class="keyword">if</span> (hasKeytab &amp;&amp; !shouldProxy) &#123; </span><br><span class="line">	  <span class="keyword">return</span> HadoopSecureHiveWrapper2<span class="class">.<span class="keyword">class</span>.<span class="title">getName</span>()</span>;</span><br><span class="line">	&#125;</span><br><span class="line">    <span class="keyword">return</span> HADOOP_SECURE_HIVE_WRAPPER;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>实现HadoopSecureHiveWrapper 类，类的功能就是新建一个JDBC链接，执行sql，并且异步轮询询问日志。代码比较简单冗长，这里就不贴了。</li>
</ul>
<h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><p>[1]<a href="https://cwiki.apache.org/confluence/display/Hive/HiveServer2+Overview" target="_blank" rel="noopener">https://cwiki.apache.org/confluence/display/Hive/HiveServer2+Overview</a><br>[2]<a href="https://hive.apache.org/" target="_blank" rel="noopener">https://hive.apache.org/</a><br>[3]<a href="https://github.com/azkaban/azkaban-plugins" target="_blank" rel="noopener">https://github.com/azkaban/azkaban-plugins</a><br>[4]AZ开发文档：<a href="http://azkaban.github.io/azkaban/docs/latest/#overview" target="_blank" rel="noopener">http://azkaban.github.io/azkaban/docs/latest/#overview</a><br>[5]强子哥的源码分析：<a href="https://my.oschina.net/qiangzigege/blog/653198" target="_blank" rel="noopener">https://my.oschina.net/qiangzigege/blog/653198</a><br>[6]azkaban源码： git clone <a href="https://github.com/azkaban/azkaban.git" target="_blank" rel="noopener">https://github.com/azkaban/azkaban.git</a></p>
]]></content>
      <categories>
        <category>Azkaban</category>
      </categories>
      <tags>
        <tag>Azkaban</tag>
        <tag>源码分析</tag>
      </tags>
  </entry>
  <entry>
    <title>Azkaban Learning</title>
    <url>/2017/01/11/Azkaban%E5%AD%A6%E4%B9%A0/</url>
    <content><![CDATA[<h1 id="Azkaban"><a href="#Azkaban" class="headerlink" title="Azkaban"></a>Azkaban</h1><p>关键字：Azkaban简介、大数据作业调度系统</p>
<p>这篇文章适合对azkaban有一定了解的人阅读。建议先粗读：<br>AZ开发文档：<a href="http://azkaban.github.io/azkaban/docs/latest/#overview" target="_blank" rel="noopener">http://azkaban.github.io/azkaban/docs/latest/#overview</a><br>强子哥的源码分析：<a href="https://my.oschina.net/qiangzigege/blog/653198" target="_blank" rel="noopener">https://my.oschina.net/qiangzigege/blog/653198</a><br>(以下内容部分摘自上两个链接)</p>
<p>azkaban源码： git clone <a href="https://github.com/azkaban/azkaban.git" target="_blank" rel="noopener">https://github.com/azkaban/azkaban.git</a></p>
<a id="more"></a>

<h2 id="Azkaban-简介"><a href="#Azkaban-简介" class="headerlink" title="Azkaban 简介"></a>Azkaban 简介</h2><blockquote>
<p><strong>Azkaban</strong> was implemented at LinkedIn to solve the problem of Hadoop job dependencies. We had jobs that needed to run in  order, from ETL jobs to data analytics products.<br>Initially a single server solution, with the increased number of Hadoop users over the years, Azkaban has evolved to be a more robust solution.</p>
</blockquote>
<blockquote>
<p>Azkaban 是由Linkedln公司为了解决hadoop 作业之间的依赖而实现的。因为有一些ETL作业以及数据分析产品需要按照一定的顺序去执行。<br>随着hadoop用户的逐年增加，Azkaban从一个简单的服务解决方案发展成为一个更加健壮鲁棒的方案。</p>
</blockquote>
<p>Azkaban的系统架构主要由三个组件组成：</p>
<ul>
<li><strong>WebServer</strong> ：暴露API，提供分发作业和调度作业功能；</li>
<li><strong>ExecServer</strong> ：对WebServer 暴露 API ，提供执行作业的功能；</li>
<li><strong>MySQL</strong> ：数据存储，实现Web 和 Exec之间的数据共享和部分状态的同步。</li>
</ul>
<p><img src="https://pic-bed-sz.oss-cn-shenzhen.aliyuncs.com/blog/azkaban%E6%9E%B6%E6%9E%84%E5%9B%BE.png" alt="azkaban架构图"></p>
<p>多执行节点模式下，更细节一点的架构图可以如下，图中省略MySQL数据库：</p>
<p><img src="https://pic-bed-sz.oss-cn-shenzhen.aliyuncs.com/blog/azkaban%E5%A4%9A%E6%89%A7%E8%A1%8C%E8%8A%82%E7%82%B9%E6%9E%B6%E6%9E%84%E5%9B%BE.png" alt="Azkaban细节图"></p>
<p>非常简单而直观</p>
<hr>
<h2 id="WebServer"><a href="#WebServer" class="headerlink" title="WebServer"></a>WebServer</h2><h3 id="暴露API"><a href="#暴露API" class="headerlink" title="暴露API"></a>暴露API</h3><p>&emsp;在azkaban-webserver工程中，可以非常清晰地看到对外暴露的Servlet，其中最主要的几个是：</p>
<ul>
<li><strong>ExecutorServlet</strong> 主要提供立即执行作业、取消作业流、暂停作业流、获取流或节点日志等接口</li>
<li><strong>ScheduleServlet</strong> 主要提供设置调度、设置Sla报警规则、获取调度信息等接口</li>
<li><strong>HistoryServlet</strong> 主要提供查看作业流执行历史的接口</li>
<li><strong>ProjectManagerServlet</strong> 主要提供上传项目zip包、下载项目zip包、删除项目、获取流的DAG等接口</li>
</ul>
<h3 id="分发作业"><a href="#分发作业" class="headerlink" title="分发作业"></a>分发作业</h3><p>&emsp;ExecutorManager 主要承担这部分的功能，所有类型的作业（包括立即执行和调度执行）,都会通过submitExecutableFlow(ExecutableFlow exflow, String userId)这个方法进行提交。</p>
<p>&emsp;在该方法中，我们可以看到：如果是多执行节点模式下，执行实例先放进分发队列中；如果是单节点模式下，立即调用dispatch方法进行分发。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (isMultiExecutorMode()) &#123;</span><br><span class="line">    <span class="comment">//Take MultiExecutor route</span></span><br><span class="line">    executorLoader.addActiveExecutableReference(reference);</span><br><span class="line">    queuedFlows.enqueue(exflow, reference);</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="comment">// assign only local executor we have</span></span><br><span class="line">    Executor choosenExecutor = activeExecutors.iterator().next();</span><br><span class="line">    executorLoader.addActiveExecutableReference(reference);</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        dispatch(reference, exflow, choosenExecutor);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (ExecutorManagerException e) &#123;</span><br><span class="line">        executorLoader.removeActiveExecutableReference(reference.getExecId());</span><br><span class="line">            <span class="keyword">throw</span> e;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;在多执行节点模式下，执行实例被放进分发队列。队列会被线程 QueueProcessorThread 定时处理。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* Method responsible for processing the non-dispatched flows */</span></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">processQueuedFlows</span><span class="params">(<span class="keyword">long</span> activeExecutorsRefreshWindow,</span></span></span><br><span class="line"><span class="function"><span class="params">  <span class="keyword">int</span> maxContinuousFlowProcessed)</span> <span class="keyword">throws</span> InterruptedException,</span></span><br><span class="line"><span class="function">  ExecutorManagerException </span>&#123;</span><br><span class="line">  <span class="keyword">long</span> lastExecutorRefreshTime = <span class="number">0</span>;</span><br><span class="line">  Pair&lt;ExecutionReference, ExecutableFlow&gt; runningCandidate;</span><br><span class="line">  <span class="keyword">int</span> currentContinuousFlowProcessed = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">while</span> (isActive() &amp;&amp; (runningCandidate = queuedFlows.fetchHead()) != <span class="keyword">null</span>) &#123;</span><br><span class="line">    ExecutionReference reference = runningCandidate.getFirst();</span><br><span class="line">    ExecutableFlow exflow = runningCandidate.getSecond();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">long</span> currentTime = System.currentTimeMillis();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// if we have dispatched more than maxContinuousFlowProcessed or</span></span><br><span class="line">    <span class="comment">// It has been more then activeExecutorsRefreshWindow millisec since we</span></span><br><span class="line">    <span class="comment">// refreshed</span></span><br><span class="line">    <span class="keyword">if</span> (currentTime - lastExecutorRefreshTime &gt; activeExecutorsRefreshWindow</span><br><span class="line">      || currentContinuousFlowProcessed &gt;= maxContinuousFlowProcessed) &#123;</span><br><span class="line">      <span class="comment">// Refresh executorInfo for all activeExecutors</span></span><br><span class="line">      refreshExecutors();</span><br><span class="line">      lastExecutorRefreshTime = currentTime;</span><br><span class="line">      currentContinuousFlowProcessed = <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * &lt;pre&gt;</span></span><br><span class="line"><span class="comment">     *  <span class="doctag">TODO:</span> Work around till we improve Filters to have a notion of GlobalSystemState.</span></span><br><span class="line"><span class="comment">     *        Currently we try each queued flow once to infer a global busy state</span></span><br><span class="line"><span class="comment">     * Possible improvements:-</span></span><br><span class="line"><span class="comment">     *   1. Move system level filters in refreshExecutors and sleep if we have all executors busy after refresh</span></span><br><span class="line"><span class="comment">     *   2. Implement GlobalSystemState in selector or in a third place to manage system filters. Basically</span></span><br><span class="line"><span class="comment">     *      taking out all the filters which do not depend on the flow but are still being part of Selector.</span></span><br><span class="line"><span class="comment">     * Assumptions:-</span></span><br><span class="line"><span class="comment">     *   1. no one else except QueueProcessor is updating ExecutableFlow update time</span></span><br><span class="line"><span class="comment">     *   2. re-attempting a flow (which has been tried before) is considered as all executors are busy</span></span><br><span class="line"><span class="comment">     * &lt;/pre&gt;</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">if</span>(exflow.getUpdateTime() &gt; lastExecutorRefreshTime) &#123;</span><br><span class="line">      <span class="comment">// put back in the queue</span></span><br><span class="line">      queuedFlows.enqueue(exflow, reference);</span><br><span class="line">      <span class="keyword">long</span> sleepInterval =</span><br><span class="line">        activeExecutorsRefreshWindow</span><br><span class="line">          - (currentTime - lastExecutorRefreshTime);</span><br><span class="line">      <span class="comment">// wait till next executor refresh</span></span><br><span class="line">      sleep(sleepInterval);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      exflow.setUpdateTime(currentTime);</span><br><span class="line">      <span class="comment">// process flow with current snapshot of activeExecutors</span></span><br><span class="line">      selectExecutorAndDispatchFlow(reference, exflow, <span class="keyword">new</span> HashSet&lt;Executor&gt;(activeExecutors));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// do not count failed flow processsing (flows still in queue)</span></span><br><span class="line">    <span class="keyword">if</span>(queuedFlows.getFlow(exflow.getExecutionId()) == <span class="keyword">null</span>) &#123;</span><br><span class="line">      currentContinuousFlowProcessed++;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;&emsp;selectExecutorAndDispatchFlow 方法先是选择执行节点（选择节点的实现比较有意思），选好节点后最终也是调用了dispatch进行作业分发。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* process flow with a snapshot of available Executors */</span></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">selectExecutorAndDispatchFlow</span><span class="params">(ExecutionReference reference,</span></span></span><br><span class="line"><span class="function"><span class="params">      ExecutableFlow exflow, Set&lt;Executor&gt; availableExecutors)</span></span></span><br><span class="line"><span class="function">      <span class="keyword">throws</span> ExecutorManagerException </span>&#123;</span><br><span class="line">      <span class="keyword">synchronized</span> (exflow) &#123;</span><br><span class="line">        Executor selectedExecutor = selectExecutor(exflow, availableExecutors);</span><br><span class="line">        <span class="keyword">if</span> (selectedExecutor != <span class="keyword">null</span>) &#123;</span><br><span class="line">          <span class="keyword">try</span> &#123;</span><br><span class="line">            dispatch(reference, exflow, selectedExecutor);</span><br><span class="line">          &#125; <span class="keyword">catch</span> (ExecutorManagerException e) &#123;</span><br><span class="line">            logger.warn(String.format(</span><br><span class="line">              <span class="string">"Executor %s responded with exception for exec: %d"</span>,</span><br><span class="line">              selectedExecutor, exflow.getExecutionId()), e);</span><br><span class="line">            handleDispatchExceptionCase(reference, exflow, selectedExecutor,</span><br><span class="line">              availableExecutors);</span><br><span class="line">          &#125;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">          handleNoExecutorSelectedCase(reference, exflow);</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;&emsp;因为Web 和Exec 之间是通过mysql进行数据共享的，所以dispatch进行作业分发的逻辑非常简单，就是简单地通过HTTP请求传递execId等信息，其余所需要的数据都通过数据库读写完成。</p>
<h3 id="调度作业"><a href="#调度作业" class="headerlink" title="调度作业"></a>调度作业</h3><p>&emsp;&emsp;调度作业是调度系统的最重要的功能之一，也是Azkaban里相对复杂的一个模块。调度是通过ScheduleManager对外暴露，对应着的结构是Schedule；对内是通过TriggerManager来实现，对应着的结构是Trigger。</p>
<p>&emsp;&emsp;所有的调度信息都通过ScheduleManager.scheduleFlow传入,可以看到传入参数包含了项目id、项目名字、流名字、第一次调度时间戳、时区、调度周期、下一次执行时间、提交时间、提交人。对于一个调度来说，最关键的信息无非是第一次调度时间和调度周期。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> Schedule <span class="title">scheduleFlow</span><span class="params">(<span class="keyword">final</span> <span class="keyword">int</span> scheduleId, <span class="keyword">final</span> <span class="keyword">int</span> projectId,</span></span></span><br><span class="line"><span class="function"><span class="params">      <span class="keyword">final</span> String projectName, <span class="keyword">final</span> String flowName, <span class="keyword">final</span> String status,</span></span></span><br><span class="line"><span class="function"><span class="params">      <span class="keyword">final</span> <span class="keyword">long</span> firstSchedTime, <span class="keyword">final</span> DateTimeZone timezone,</span></span></span><br><span class="line"><span class="function"><span class="params">      <span class="keyword">final</span> ReadablePeriod period, <span class="keyword">final</span> <span class="keyword">long</span> lastModifyTime,</span></span></span><br><span class="line"><span class="function"><span class="params">      <span class="keyword">final</span> <span class="keyword">long</span> nextExecTime, <span class="keyword">final</span> <span class="keyword">long</span> submitTime, <span class="keyword">final</span> String submitUser)</span></span></span><br></pre></td></tr></table></figure>

<p>&emsp;&emsp;从scheduleFlow 往下可以看到调用了TriggerBasedScheduleLoader.insertSchedule。这个方法里边先是将Schedule转换成了Trigger，然后将Trigger放到了TriggerManager里边。scheduleToTrigger方法写的非常简洁巧妙，读者自行研究，此处不作细致分析。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"> <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">insertSchedule</span><span class="params">(Schedule s)</span> <span class="keyword">throws</span> ScheduleManagerException </span>&#123;</span><br><span class="line">   Trigger t = scheduleToTrigger(s);</span><br><span class="line">   <span class="keyword">try</span> &#123;</span><br><span class="line">     triggerManager.insertTrigger(t, t.getSubmitUser());</span><br><span class="line">     s.setScheduleId(t.getTriggerId());</span><br><span class="line">   &#125; <span class="keyword">catch</span> (TriggerManagerException e) &#123;</span><br><span class="line">     <span class="keyword">throw</span> <span class="keyword">new</span> ScheduleManagerException(<span class="string">"Failed to insert new schedule!"</span>, e);</span><br><span class="line">   &#125;</span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;&emsp; 我们在继续看看Trigger被塞到TriggerManager做了些啥。从下边可以看到，先是调用triggerLoader写进数据库，然后就放到了一个线程runnerThread中去。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">insertTrigger</span><span class="params">(Trigger t)</span> <span class="keyword">throws</span> TriggerManagerException </span>&#123;</span><br><span class="line">    <span class="keyword">synchronized</span> (syncObj) &#123;</span><br><span class="line">      <span class="keyword">try</span> &#123;</span><br><span class="line">        triggerLoader.addTrigger(t);</span><br><span class="line">      &#125; <span class="keyword">catch</span> (TriggerLoaderException e) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> TriggerManagerException(e);</span><br><span class="line">      &#125;</span><br><span class="line">      runnerThread.addTrigger(t);</span><br><span class="line">      triggerIdMap.put(t.getTriggerId(), t);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;&emsp; 接下来就显而易见了，这个线程TriggerScannerThread runnerThread 定期检查Trigger是否应该触发（onTriggerTrigger）或者终止（onTriggerExpire）。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">checkAllTriggers</span><span class="params">()</span> <span class="keyword">throws</span> TriggerManagerException </span>&#123;</span><br><span class="line">  <span class="keyword">long</span> now = System.currentTimeMillis();</span><br><span class="line"></span><br><span class="line">  <span class="comment">// sweep through the rest of them</span></span><br><span class="line">  <span class="keyword">for</span> (Trigger t : triggers) &#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">      scannerStage = <span class="string">"Checking for trigger "</span> + t.getTriggerId();</span><br><span class="line"></span><br><span class="line">      <span class="keyword">boolean</span> shouldSkip = <span class="keyword">true</span>;</span><br><span class="line">      <span class="keyword">if</span> (shouldSkip &amp;&amp; t.getInfo() != <span class="keyword">null</span> &amp;&amp; t.getInfo().containsKey(<span class="string">"monitored.finished.execution"</span>)) &#123;</span><br><span class="line">        <span class="keyword">int</span> execId = Integer.valueOf((String) t.getInfo().get(<span class="string">"monitored.finished.execution"</span>));</span><br><span class="line">        <span class="keyword">if</span> (justFinishedFlows.containsKey(execId)) &#123;</span><br><span class="line">          logger.info(<span class="string">"Monitored execution has finished. Checking trigger earlier "</span> + t.getTriggerId());</span><br><span class="line">          shouldSkip = <span class="keyword">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">if</span> (shouldSkip &amp;&amp; t.getNextCheckTime() &gt; now) &#123;</span><br><span class="line">        shouldSkip = <span class="keyword">false</span>;</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      <span class="keyword">if</span> (shouldSkip) &#123;</span><br><span class="line">        logger.info(<span class="string">"Skipping trigger"</span> + t.getTriggerId() + <span class="string">" until "</span> + t.getNextCheckTime());</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      <span class="keyword">if</span> (logger.isDebugEnabled()) &#123;</span><br><span class="line">        logger.info(<span class="string">"Checking trigger "</span> + t.getTriggerId());</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">if</span> (t.getStatus().equals(TriggerStatus.READY)) &#123;</span><br><span class="line">        <span class="keyword">if</span> (t.triggerConditionMet()) &#123;</span><br><span class="line">          onTriggerTrigger(t);</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (t.expireConditionMet()) &#123;</span><br><span class="line">          onTriggerExpire(t);</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">if</span> (t.getStatus().equals(TriggerStatus.EXPIRED) &amp;&amp; t.getSource().equals(<span class="string">"azkaban"</span>)) &#123;</span><br><span class="line">        removeTrigger(t);</span><br><span class="line">      &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        t.updateNextCheckTime();</span><br><span class="line">      &#125;</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Throwable th) &#123;</span><br><span class="line">      <span class="comment">//skip this trigger, moving on to the next one</span></span><br><span class="line">      logger.error(<span class="string">"Failed to process trigger with id : "</span> + t.getTriggerId(), th);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;&emsp; Trigger触发的时候就会调用自己的action.doAction()，调度任务的Trigger的action一般都是ExecuteFlowAction，其doAction方法如下。方法主要做了两个事情，第一个是构建执行实例ExecutableFlow，第二个是如果该调度设置了报警规则，则构建SlaTrigger。</p>
<p>&emsp;&emsp; 构建执行实例完成后，可以看到调用了executorManager.submitExecutableFlow(exflow, submitUser) 进行作业分发，这样子，就跟上文提到的作业分发殊途同归。下边不再分析。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">doAction</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (projectManager == <span class="keyword">null</span> || executorManager == <span class="keyword">null</span>) &#123;</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> Exception(<span class="string">"ExecuteFlowAction not properly initialized!"</span>);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  Project project = projectManager.getProject(projectId);</span><br><span class="line">  <span class="keyword">if</span> (project == <span class="keyword">null</span>) &#123;</span><br><span class="line">    logger.error(<span class="string">"Project to execute "</span> + projectId + <span class="string">" does not exist!"</span>);</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> RuntimeException(<span class="string">"Error finding the project to execute "</span></span><br><span class="line">        + projectId);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  Flow flow = project.getFlow(flowName);</span><br><span class="line">  <span class="keyword">if</span> (flow == <span class="keyword">null</span>) &#123;</span><br><span class="line">    logger.error(<span class="string">"Flow "</span> + flowName + <span class="string">" cannot be found in project "</span></span><br><span class="line">        + project.getName());</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> RuntimeException(<span class="string">"Error finding the flow to execute "</span></span><br><span class="line">        + flowName);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  ExecutableFlow exflow = <span class="keyword">new</span> ExecutableFlow(project, flow);</span><br><span class="line">  exflow.setSubmitUser(submitUser);</span><br><span class="line">  exflow.addAllProxyUsers(project.getProxyUsers());</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (executionOptions == <span class="keyword">null</span>) &#123;</span><br><span class="line">    executionOptions = <span class="keyword">new</span> ExecutionOptions();</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">if</span> (!executionOptions.isFailureEmailsOverridden()) &#123;</span><br><span class="line">    executionOptions.setFailureEmails(flow.getFailureEmails());</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">if</span> (!executionOptions.isSuccessEmailsOverridden()) &#123;</span><br><span class="line">    executionOptions.setSuccessEmails(flow.getSuccessEmails());</span><br><span class="line">  &#125;</span><br><span class="line">  exflow.setExecutionOptions(executionOptions);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">try</span> &#123;</span><br><span class="line">    executorManager.submitExecutableFlow(exflow, submitUser);</span><br><span class="line">    logger.info(<span class="string">"Invoked flow "</span> + project.getName() + <span class="string">"."</span> + flowName);</span><br><span class="line">  &#125; <span class="keyword">catch</span> (ExecutorManagerException e) &#123;</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> RuntimeException(e);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// deal with sla</span></span><br><span class="line">  <span class="keyword">if</span> (slaOptions != <span class="keyword">null</span> &amp;&amp; slaOptions.size() &gt; <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="keyword">int</span> execId = exflow.getExecutionId();</span><br><span class="line">    <span class="keyword">for</span> (SlaOption sla : slaOptions) &#123;</span><br><span class="line">      logger.info(<span class="string">"Adding sla trigger "</span> + sla.toString() + <span class="string">" to execution "</span></span><br><span class="line">          + execId);</span><br><span class="line">      SlaChecker slaFailChecker =</span><br><span class="line">          <span class="keyword">new</span> SlaChecker(<span class="string">"slaFailChecker"</span>, sla, execId);</span><br><span class="line">      Map&lt;String, ConditionChecker&gt; slaCheckers =</span><br><span class="line">          <span class="keyword">new</span> HashMap&lt;String, ConditionChecker&gt;();</span><br><span class="line">      slaCheckers.put(slaFailChecker.getId(), slaFailChecker);</span><br><span class="line">      Condition triggerCond =</span><br><span class="line">          <span class="keyword">new</span> Condition(slaCheckers, slaFailChecker.getId()</span><br><span class="line">              + <span class="string">".isSlaFailed()"</span>);</span><br><span class="line">      <span class="comment">// if whole flow finish before violate sla, just expire</span></span><br><span class="line">      SlaChecker slaPassChecker =</span><br><span class="line">          <span class="keyword">new</span> SlaChecker(<span class="string">"slaPassChecker"</span>, sla, execId);</span><br><span class="line">      Map&lt;String, ConditionChecker&gt; expireCheckers =</span><br><span class="line">          <span class="keyword">new</span> HashMap&lt;String, ConditionChecker&gt;();</span><br><span class="line">      expireCheckers.put(slaPassChecker.getId(), slaPassChecker);</span><br><span class="line">      Condition expireCond =</span><br><span class="line">          <span class="keyword">new</span> Condition(expireCheckers, slaPassChecker.getId()</span><br><span class="line">              + <span class="string">".isSlaPassed()"</span>);</span><br><span class="line">      List&lt;TriggerAction&gt; actions = <span class="keyword">new</span> ArrayList&lt;TriggerAction&gt;();</span><br><span class="line">      List&lt;String&gt; slaActions = sla.getActions();</span><br><span class="line">      <span class="keyword">for</span> (String act : slaActions) &#123;</span><br><span class="line">        <span class="keyword">if</span> (act.equals(SlaOption.ACTION_ALERT)) &#123;</span><br><span class="line">          SlaAlertAction slaAlert =</span><br><span class="line">              <span class="keyword">new</span> SlaAlertAction(<span class="string">"slaAlert"</span>, sla, execId);</span><br><span class="line">          actions.add(slaAlert);</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (act.equals(SlaOption.ACTION_CANCEL_FLOW)) &#123;</span><br><span class="line">          KillExecutionAction killAct =</span><br><span class="line">              <span class="keyword">new</span> KillExecutionAction(<span class="string">"killExecution"</span>, execId);</span><br><span class="line">          actions.add(killAct);</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">      Trigger slaTrigger =</span><br><span class="line">          <span class="keyword">new</span> Trigger(<span class="string">"azkaban_sla"</span>, <span class="string">"azkaban"</span>, triggerCond, expireCond,</span><br><span class="line">              actions);</span><br><span class="line">      slaTrigger.getInfo().put(<span class="string">"monitored.finished.execution"</span>,</span><br><span class="line">          String.valueOf(execId));</span><br><span class="line">      slaTrigger.setResetOnTrigger(<span class="keyword">false</span>);</span><br><span class="line">      slaTrigger.setResetOnExpire(<span class="keyword">false</span>);</span><br><span class="line">      logger.info(<span class="string">"Ready to put in the sla trigger"</span>);</span><br><span class="line">      triggerManager.insertTrigger(slaTrigger);</span><br><span class="line">      logger.info(<span class="string">"Sla inserted."</span>);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="WebServer总结"><a href="#WebServer总结" class="headerlink" title="WebServer总结"></a>WebServer总结</h3><p>&emsp;&emsp; 下边用一张图简单总结</p>
<p><img src="https://pic-bed-sz.oss-cn-shenzhen.aliyuncs.com/blog/%E4%BD%9C%E4%B8%9A%E5%88%86%E5%8F%91%E8%B0%83%E5%BA%A6%E6%B5%81%E7%A8%8B%E5%9B%BE.png" alt="作业分发调度流程图.png"></p>
<h2 id="ExecServer"><a href="#ExecServer" class="headerlink" title="ExecServer"></a>ExecServer</h2><h3 id="暴露Restful-API"><a href="#暴露Restful-API" class="headerlink" title="暴露Restful API"></a>暴露Restful API</h3><p>&emsp;&emsp;Azkaban3.0后就开始支持多执行节点部署。单个执行节点比较简单，对web暴露的API也比较少，主要是：</p>
<ul>
<li><strong>ExecutorServlet</strong> 主要提供执行、取消、暂停、日志查询等接口。</li>
</ul>
<h3 id="执行作业"><a href="#执行作业" class="headerlink" title="执行作业"></a>执行作业</h3><p>&emsp;&emsp;这里简单看下执行节点执行一个作业的流程是怎样的。我们在ExecutorServlet中看到所有的执行作业请求都经过handleAjaxExecute方法，这个方法简单地将执行id传递给FlowRunnerManager：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">handleAjaxExecute</span><span class="params">(HttpServletRequest req,</span></span></span><br><span class="line"><span class="function"><span class="params">      Map&lt;String, Object&gt; respMap, <span class="keyword">int</span> execId)</span> <span class="keyword">throws</span> ServletException </span>&#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">      flowRunnerManager.submitFlow(execId);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (ExecutorManagerException e) &#123;</span><br><span class="line">      e.printStackTrace();</span><br><span class="line">      logger.error(e);</span><br><span class="line">      respMap.put(RESPONSE_ERROR, e.getMessage());</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;FlowRunnerManager 通过submitFlow方法提交工作流去执行。先是构建执行实例ExecutableFlow，然后准备执行目录setupFlow(flow)，然后生成FlowRunner，然后提交到线程池去运行executorService.submit(runner)。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">submitFlow</span><span class="params">(<span class="keyword">int</span> execId)</span> <span class="keyword">throws</span> ExecutorManagerException </span>&#123;</span><br><span class="line">   <span class="comment">// Load file and submit</span></span><br><span class="line">   <span class="keyword">if</span> (runningFlows.containsKey(execId)) &#123;</span><br><span class="line">     <span class="keyword">throw</span> <span class="keyword">new</span> ExecutorManagerException(<span class="string">"Execution "</span> + execId</span><br><span class="line">         + <span class="string">" is already running."</span>);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   ExecutableFlow flow = <span class="keyword">null</span>;</span><br><span class="line">   flow = executorLoader.fetchExecutableFlow(execId);</span><br><span class="line">   <span class="keyword">if</span> (flow == <span class="keyword">null</span>) &#123;</span><br><span class="line">     <span class="keyword">throw</span> <span class="keyword">new</span> ExecutorManagerException(<span class="string">"Error loading flow with exec "</span></span><br><span class="line">         + execId);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Sets up the project files and execution directory.</span></span><br><span class="line">   setupFlow(flow);</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Setup flow runner</span></span><br><span class="line">   FlowWatcher watcher = <span class="keyword">null</span>;</span><br><span class="line">   ExecutionOptions options = flow.getExecutionOptions();</span><br><span class="line">   <span class="keyword">if</span> (options.getPipelineExecutionId() != <span class="keyword">null</span>) &#123;</span><br><span class="line">     Integer pipelineExecId = options.getPipelineExecutionId();</span><br><span class="line">     FlowRunner runner = runningFlows.get(pipelineExecId);</span><br><span class="line"></span><br><span class="line">     <span class="keyword">if</span> (runner != <span class="keyword">null</span>) &#123;</span><br><span class="line">       watcher = <span class="keyword">new</span> LocalFlowWatcher(runner);</span><br><span class="line">     &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">       watcher = <span class="keyword">new</span> RemoteFlowWatcher(pipelineExecId, executorLoader);</span><br><span class="line">     &#125;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="keyword">int</span> numJobThreads = numJobThreadPerFlow;</span><br><span class="line">   <span class="keyword">if</span> (options.getFlowParameters().containsKey(FLOW_NUM_JOB_THREADS)) &#123;</span><br><span class="line">     <span class="keyword">try</span> &#123;</span><br><span class="line">       <span class="keyword">int</span> numJobs =</span><br><span class="line">           Integer.valueOf(options.getFlowParameters().get(</span><br><span class="line">               FLOW_NUM_JOB_THREADS));</span><br><span class="line">       <span class="keyword">if</span> (numJobs &gt; <span class="number">0</span> &amp;&amp; (numJobs &lt;= numJobThreads || ProjectWhitelist</span><br><span class="line">               .isProjectWhitelisted(flow.getProjectId(),</span><br><span class="line">                   WhitelistType.NumJobPerFlow))) &#123;</span><br><span class="line">         numJobThreads = numJobs;</span><br><span class="line">       &#125;</span><br><span class="line">     &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">       <span class="keyword">throw</span> <span class="keyword">new</span> ExecutorManagerException(</span><br><span class="line">           <span class="string">"Failed to set the number of job threads "</span></span><br><span class="line">               + options.getFlowParameters().get(FLOW_NUM_JOB_THREADS)</span><br><span class="line">               + <span class="string">" for flow "</span> + execId, e);</span><br><span class="line">     &#125;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   FlowRunner runner =</span><br><span class="line">       <span class="keyword">new</span> FlowRunner(flow, executorLoader, projectLoader, jobtypeManager);</span><br><span class="line">   runner.setFlowWatcher(watcher)</span><br><span class="line">       .setJobLogSettings(jobLogChunkSize, jobLogNumFiles)</span><br><span class="line">       .setValidateProxyUser(validateProxyUser)</span><br><span class="line">       .setNumJobThreads(numJobThreads).addListener(<span class="keyword">this</span>);</span><br><span class="line"></span><br><span class="line">   configureFlowLevelMetrics(runner);</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Check again.</span></span><br><span class="line">   <span class="keyword">if</span> (runningFlows.containsKey(execId)) &#123;</span><br><span class="line">     <span class="keyword">throw</span> <span class="keyword">new</span> ExecutorManagerException(<span class="string">"Execution "</span> + execId</span><br><span class="line">         + <span class="string">" is already running."</span>);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Finally, queue the sucker.</span></span><br><span class="line">   runningFlows.put(execId, runner);</span><br><span class="line"></span><br><span class="line">   <span class="keyword">try</span> &#123;</span><br><span class="line">     <span class="comment">// The executorService already has a queue.</span></span><br><span class="line">     <span class="comment">// The submit method below actually returns an instance of FutureTask,</span></span><br><span class="line">     <span class="comment">// which implements interface RunnableFuture, which extends both</span></span><br><span class="line">     <span class="comment">// Runnable and Future interfaces</span></span><br><span class="line">     Future&lt;?&gt; future = executorService.submit(runner);</span><br><span class="line">     <span class="comment">// keep track of this future</span></span><br><span class="line">     submittedFlows.put(future, runner.getExecutionId());</span><br><span class="line">     <span class="comment">// update the last submitted time.</span></span><br><span class="line">     <span class="keyword">this</span>.lastFlowSubmittedDate = System.currentTimeMillis();</span><br><span class="line">   &#125; <span class="keyword">catch</span> (RejectedExecutionException re) &#123;</span><br><span class="line">     <span class="keyword">throw</span> <span class="keyword">new</span> ExecutorManagerException(</span><br><span class="line">         <span class="string">"Azkaban server can't execute any more flows. "</span></span><br><span class="line">             + <span class="string">"The number of running flows has reached the system configured limit."</span></span><br><span class="line">             + <span class="string">"Please notify Azkaban administrators"</span>);</span><br><span class="line">   &#125;</span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp; FlowRunner本身也继承与Runnable，其run方法里边调用了 runFlow方法，方法内容如下。方法里按照树的层次结构逐层访问DAG图的每一个job，逐个去提交执行。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">runFlow</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">    logger.info(<span class="string">"Starting flows"</span>);</span><br><span class="line">    runReadyJob(<span class="keyword">this</span>.flow);</span><br><span class="line">    updateFlow();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (!flowFinished) &#123;</span><br><span class="line">      <span class="keyword">synchronized</span> (mainSyncObj) &#123;</span><br><span class="line">        <span class="keyword">if</span> (flowPaused) &#123;</span><br><span class="line">          <span class="keyword">try</span> &#123;</span><br><span class="line">            mainSyncObj.wait(CHECK_WAIT_MS);</span><br><span class="line">          &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">          &#125;</span><br><span class="line"></span><br><span class="line">          <span class="keyword">continue</span>;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">          <span class="keyword">if</span> (retryFailedJobs) &#123;</span><br><span class="line">            retryAllFailures();</span><br><span class="line">          &#125; <span class="keyword">else</span> <span class="keyword">if</span> (!progressGraph()) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">              mainSyncObj.wait(CHECK_WAIT_MS);</span><br><span class="line">            &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">            &#125;</span><br><span class="line">          &#125;</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    logger.info(<span class="string">"Finishing up flow. Awaiting Termination"</span>);</span><br><span class="line">    executorService.shutdown();</span><br><span class="line"></span><br><span class="line">    updateFlow();</span><br><span class="line">    logger.info(<span class="string">"Finished Flow"</span>);</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;&emsp;  对于单个job，最后构造一个JobRunner去执行之。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">runExecutableNode</span><span class="params">(ExecutableNode node)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">    <span class="comment">// Collect output props from the job's dependencies.</span></span><br><span class="line">    prepareJobProperties(node);</span><br><span class="line"></span><br><span class="line">    node.setStatus(Status.QUEUED);</span><br><span class="line">    JobRunner runner = createJobRunner(node);</span><br><span class="line">    logger.info(<span class="string">"Submitting job '"</span> + node.getNestedId() + <span class="string">"' to run."</span>);</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">      executorService.submit(runner);</span><br><span class="line">      activeJobRunners.add(runner);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (RejectedExecutionException e) &#123;</span><br><span class="line">      logger.error(e);</span><br><span class="line">    &#125;</span><br><span class="line">    ;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line"> <span class="function"><span class="keyword">private</span> JobRunner <span class="title">createJobRunner</span><span class="params">(ExecutableNode node)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// Load job file.</span></span><br><span class="line">    File path = <span class="keyword">new</span> File(execDir, node.getJobSource());</span><br><span class="line"></span><br><span class="line">    JobRunner jobRunner =</span><br><span class="line">        <span class="keyword">new</span> JobRunner(node, path.getParentFile(), executorLoader,</span><br><span class="line">            jobtypeManager);</span><br><span class="line">    <span class="keyword">if</span> (watcher != <span class="keyword">null</span>) &#123;</span><br><span class="line">      jobRunner.setPipeline(watcher, pipelineLevel);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (validateUserProxy) &#123;</span><br><span class="line">      jobRunner.setValidatedProxyUsers(proxyUsers);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    jobRunner.setDelayStart(node.getDelayedExecution());</span><br><span class="line">    jobRunner.setLogSettings(logger, jobLogFileSize, jobLogNumFiles);</span><br><span class="line">    jobRunner.addListener(listener);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (JobCallbackManager.isInitialized()) &#123;</span><br><span class="line">      jobRunner.addListener(JobCallbackManager.getInstance());</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    configureJobLevelMetrics(jobRunner);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> jobRunner;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>


<p>&emsp;&emsp;每个jobRunner在执行的时候，都去插件模块里边寻找对应的插件来进行job的类型加载。每种job类型都有对应的run方法。最后就是调用run方法去执行job。各种不同类型的job可以参考azkaban默认的job类型以及 azkaban-plugin工程里边实现的一些hadoop相关作业类型。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">       job = jobtypeManager.buildJobExecutor(<span class="keyword">this</span>.jobId, props, logger);</span><br><span class="line">     &#125; <span class="keyword">catch</span> (JobTypeManagerException e) &#123;</span><br><span class="line">       logger.error(<span class="string">"Failed to build job type"</span>, e);</span><br><span class="line">       <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">     &#125;</span><br></pre></td></tr></table></figure>

<h2 id="Azkaban-Plugin"><a href="#Azkaban-Plugin" class="headerlink" title="Azkaban Plugin"></a>Azkaban Plugin</h2><p>&emsp;&emsp;azkaban的插件机制使得可以非常方便的增加插件类型，从而支持运行更多的作业类型。azkaban的hadoop插件可以从以下仓库中找到：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git <span class="built_in">clone</span> https://github.com/azkaban/azkaban-plugins.git</span><br></pre></td></tr></table></figure>

<h3 id="插件的实现"><a href="#插件的实现" class="headerlink" title="插件的实现"></a>插件的实现</h3><p>&emsp;&emsp; 其中插件的类继承关系图如下。每种插件作业都会单独起一个进程去执行。其中ProcessJob就是负责起进程的一个类；JavaProcessJob继承自它，特化为Java进程；其他的hadoop插件又各自继承自JavaProcessJob。如果要自己实现插件类型，只要继承JavaProcessJob类，在继承子类里边调用插件的Wrapper类就可以了。具体细节可以看代码实现。</p>
<p><img src="https://pic-bed-sz.oss-cn-shenzhen.aliyuncs.com/blog/azkaban%E6%8F%92%E4%BB%B6%E5%AE%9E%E7%8E%B0%E5%9B%BE.png" alt="azkaban插件实现图"></p>
]]></content>
      <categories>
        <category>调度系统</category>
      </categories>
      <tags>
        <tag>Azkaban</tag>
        <tag>大数据组件</tag>
      </tags>
  </entry>
</search>
